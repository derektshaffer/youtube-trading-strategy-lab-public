name: One-Off Archetype Transfer Experiment

on:
  push:
    branches:
      - feature/ml-stock-archetypes-20260829

permissions:
  contents: read

jobs:
  run-ml:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      ALPACA_API_KEY: ${{ secrets.ALPACA_API_KEY }}
      ALPACA_SECRET_KEY: ${{ secrets.ALPACA_SECRET_KEY }}
      ALPACA_LIVE_FEED: ${{ vars.ALPACA_LIVE_FEED || 'iex' }}
      ALPACA_HISTORICAL_FEED: ${{ vars.ALPACA_HISTORICAL_FEED || 'sip' }}

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.14"
          cache: pip
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements.txt
      - name: Validate Alpaca secrets
        shell: bash
        run: |
          test -n "$ALPACA_API_KEY"
          test -n "$ALPACA_SECRET_KEY"
      - name: Run 20-stock archetype transfer experiment
        run: python oneoff_archetype_transfer_experiment.py
      - name: Upload result
        uses: actions/upload-artifact@v4
        with:
          name: archetype-transfer-experiment-result
          path: archetype_transfer_experiment_result.json
          retention-days: 7
